'use strict';

/**
 * accept-sos-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::accept-sos-request.accept-sos-request');
